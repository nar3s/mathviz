manim>=0.18.0
typer[all]>=0.9.0
pyyaml>=6.0
requests
sarvamai>=0.1.25
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0.0

# FastAPI backend
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydub>=0.25.1
httpx>=0.27.0

# LLM providers (install the one you use)
anthropic>=0.40.0        # for LLM_PROVIDER=claude  (default)
# openai>=1.0.0          # for LLM_PROVIDER=openai  (uncomment to enable)
# google-genai>=1.0.0    # for LLM_PROVIDER=gemini  (uncomment to enable)

# Cloud storage
boto3>=1.34.0            # for Cloudflare R2 (S3-compatible)
