[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: fast isolated tests with no external dependencies
    integration: tests that do real I/O with mocked external APIs
    slow: requires Manim or FFmpeg installed; skip in CI if unavailable
