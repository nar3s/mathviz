# ── LLM Provider ──────────────────────────────────────────────────────────────
LLM_PROVIDER=claude          # claude | openai
LLM_MODEL=claude-opus-4-6    # model ID for the selected provider
LLM_API_KEY=                 # your Anthropic or OpenAI key

# ── Sarvam AI TTS ─────────────────────────────────────────────────────────────
SARVAM_API_KEY=              # your Sarvam AI key

# ── Output ────────────────────────────────────────────────────────────────────
# On Railway: set to /data (the mounted persistent volume path)
# OUTPUT_DIR=/data

# ── Cloudflare R2 Storage (free 10 GB, no egress fees) ────────────────────────
# Create bucket at dash.cloudflare.com → R2 → New bucket
# Enable "Public Access" on the bucket to get a pub-xxx.r2.dev URL
# Generate API token: R2 → Manage R2 API Tokens → Object Read & Write
# R2_ACCOUNT_ID=your_cloudflare_account_id
# R2_ACCESS_KEY_ID=your_r2_token_access_key
# R2_SECRET_ACCESS_KEY=your_r2_token_secret
# R2_BUCKET_NAME=mathviz
# R2_PUBLIC_URL=https://pub-xxxxxxxxxxxx.r2.dev

# ── Optional overrides ────────────────────────────────────────────────────────
# DEFAULT_VOICE=shubh
# DEFAULT_LANGUAGE=en
# MAX_RENDER_WORKERS=4
